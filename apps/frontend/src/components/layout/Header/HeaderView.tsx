import styles from './HeaderView.module.scss'

const HeaderView = () => {
  return (
    <header className={styles.header}>
      <h1>datarize-project</h1>
    </header>
  )
}

export default HeaderView
